программа на JS это последовательность инструкций,а инструкция в свою очереде это законченная запись, команда которую мы даем на выполнения
каждую новую инструкцию принято записываь с новой строки и заканчивать запись ;  точкоц с запятой 
иснтрукции JS могут состоять из:
знвчений;
операторов;
выражений;
ключевые слова;
комментарии;
JAVA SCRIPT есть несколько типов значений (values); 
25 - литерал целого числа 
23.8 литерал дробного числа
'javasript'литерал строки 
"javascript" литерал строки
[] литерал массива
[12,7,89] литерал массива
{} литерал обьекта
{
	name:'freelanser',
	surname: "in life"
} литерал обьекта
(|) литерал регулярного выражения 
в языках переменных константы и переменные используются для хранения значений данных 
const MAX_VALUE = 17; константа
var section = 'JS'; пременная
let arr =['HTML', 'CSS', 'JS'] переменная  
ОПЕРАТОРЫ предназначены для того, чтобы давать команду. они могут быть таким
= присвоение 
+ сложение
-
console.log('lesson' + 'JS') пример сложения строк 
ВЫРАЖЕНИЯ (expression) это комбинации значений, переменных и операторов, которые присваивают переменной значение либо возвращают значение без его присваивания 
примеры выражения
КЛЮЧВЫЕ СЛОВА - иструкции JAVASCRIPT часто начинаются с ключевого слова (keyword) . оно предназначенно для того, чтобы определить какое действие джава скрипту необходимо выполнить 
let num = 'learning JS';
console.log(num); указывает браузеру создать переменную  
БЛОК ИНСТРУКЦИЙ -  джава скрипт иснтрукции можно сгруппировать с помощью блока , по сути двух фигурных скобок { } какправило используется в функциях, циклвх и д т
КОМЕНТАРИИ не все иснтрукции джаыва скрипт являются выполнимыми , код после двойных черт // или между /* */ рассматривается как комментарий
/* */ многомстрочный комментарий. влаживать многострочные комментарий в друг друга запрещенно
// комментарий 
ЧУВСТВИТЕЛЬНОСТЬ К РЕГИСТРУ 
JavaScript является регистрозависимым языком. Это значит что ключевые слова переменные именна функции и другие идентификаторы языка должны содержать одинаковые наборы прописных и строчных букв 
ПРОБЕЛЫ обязательно ставитть вокруг опраторов
Нужно избегать строк свыше 80символов
разрывать строку перед оператором